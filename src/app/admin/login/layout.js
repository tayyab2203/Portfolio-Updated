export const metadata = {
  title: 'Admin Login | Portfolio Admin Panel',
  description: 'Secure admin login for portfolio management',
};

export default function LoginLayout({ children }) {
  return children;
}
